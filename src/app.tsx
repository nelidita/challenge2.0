import { ContactList } from "./pages/contactList.page";

export const App = () => {
  return (
    <div className="h-screen w-screen bg-cover bg-center bg-burst flex justify-center">
      <ContactList />
    </div>
  );
};
